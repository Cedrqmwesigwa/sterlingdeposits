// This file is no longer needed as the hero content is directly in src/app/page.tsx
// for the "Sterling Contractors" theme.
// Keeping the file to prevent build errors if imported elsewhere, but it should be removed.
export default function HeroSection() {
  return null;
}
